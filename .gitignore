# Dependencies and Builds
/node_modules
/.next/
/out/
/dist/
/coverage

# Environment
.env
.env.local
.env.development.local
.env.production.local
.env.test.local
.env.staging.local # Added common staging environment
env_file.txt        # Never commit ad-hoc env dumps (this file leaked a live DB URL once)
env_file*.txt       # …and any variant of it

# Build Artifacts
/public/build
/public/next-auth-static # Added NextAuth specific artifacts

# Prisma Artifacts
/prisma/dev.db # Excludes SQLite development DBs if used
/prisma/*.sqlite
/prisma/migrations/ # Excludes migration files if you use 'prisma migrate' (Use 'db push' instead for simplicity)

# TypeScript
*.tsbuildinfo

# Logs & Cache..
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
.DS_Store # Added Mac OS cache files (important for Mac users like you).DS_Store
public/pdfjs/
